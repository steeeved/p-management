"use client";

import LayoutDashboard from "@/app/layoutDashboard";
import MainPage from "./mainPage";

const HomePage = () => {
  return (
    <LayoutDashboard>
      <MainPage />
    </LayoutDashboard>
  );
};

export default HomePage;
